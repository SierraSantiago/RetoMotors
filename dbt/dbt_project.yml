name: "reto_analista_ia"
version: "1.0.0"
config-version: 2

profile: "reto_analista_ia"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  reto_analista_ia:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate

seeds:
  reto_analista_ia:
    +schema: intermediate
